# ResizeToMethodJS
This JavaScript project explores some of the features of the resizeTo() method.

Objective:
Examine some additional methods that can be used with the window object. See if you can locate the resizeTo()method and use it to resize a window to be 200 pixels wide and 400 pixels high. 

